# ===== Database =====
MONGODB_URI="mongodb://atlas-sql-689b036574d9477bcb7a73b0-syluzr.a.query.mongodb.net/myVirtualDatabase?ssl=true&authSource=admin"

# ===== Authentication =====
NEXTAUTH_SECRET="generated_secure_random_string_32+_chars"
NEXTAUTH_URL="https://your-production-domain.com"  # Must match your deployed URL
JWT_SECRET="another_secure_random_string"

# ===== Email (SMTP) =====
SMTP_HOST="smtp.your-email-provider.com"  # e.g., smtp.sendgrid.net, smtp.gmail.com
SMTP_PORT="587"  # TLS port (465 for SSL)
SMTP_USER="your-smtp-username"
SMTP_PASSWORD="your-smtp-password"
SMTP_FROM="no-reply@your-domain.com"  # Must match your domain

# ===== Production-Specific =====
NODE_ENV="production"
# Enable HTTPS (if not handled by a reverse proxy like Nginx)
HTTPS="true"
# Next.js standalone (if using `output: 'standalone'`)
# NEXT_TELEMETRY_DISABLED="1"  # Optional: Disable Next.js telemetry